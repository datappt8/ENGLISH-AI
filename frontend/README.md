# English Quest - Frontend

React + TypeScript + Phaser 3 前端应用

## 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Phaser 3** - 游戏引擎
- **Vite** - 构建工具
- **React Router** - 路由管理
- **Axios** - HTTP 客户端
- **Zustand** - 状态管理

## 开发环境设置

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env` 并配置：

```bash
cp .env.example .env
```

### 3. 启动开发服务器

```bash
npm run dev
```

应用将在 http://localhost:3000 启动

## 项目结构

```
src/
├── assets/          # 静态资源（图片、音频）
├── components/      # React 组件
├── pages/           # 页面组件
├── scenes/          # Phaser 游戏场景
├── services/        # API 服务
├── stores/          # 状态管理
├── types/           # TypeScript 类型定义
├── utils/           # 工具函数
├── App.tsx          # 应用根组件
└── main.tsx         # 应用入口
```

## 可用脚本

- `npm run dev` - 启动开发服务器
- `npm run build` - 构建生产版本
- `npm run preview` - 预览生产构建
- `npm run lint` - 运行 ESLint
- `npm run type-check` - TypeScript 类型检查

## 功能特性

- ✅ 用户认证（注册/登录）
- ✅ 游戏主界面
- ✅ 个人资料页面
- 🚧 AI 对话系统
- 🚧 语音识别
- 🚧 任务系统
- 🚧 排行榜

## 开发注意事项

1. 所有 API 调用都通过 `services/` 目录中的服务进行
2. 使用 TypeScript 类型确保类型安全
3. 遵循 React Hooks 最佳实践
4. Phaser 游戏逻辑放在 `scenes/` 目录
