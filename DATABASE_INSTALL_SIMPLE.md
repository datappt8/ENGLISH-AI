# 🚀 数据库安装 - 最简单方法

## 使用 pgAdmin 4（5分钟完成）

### 步骤1: 打开 pgAdmin 4
- 开始菜单搜索 "pgAdmin 4"
- 输入你的主密码（第一次打开时设置的）

### 步骤2: 连接到 PostgreSQL
- 左侧展开 "Servers" → "PostgreSQL 18"
- 输入密码：`Englishai`（你安装时设置的）

### 步骤3: 运行快速安装脚本
1. 右键 "PostgreSQL 18" → "Query Tool"
2. 打开文件：`D:\ENGLISH-AI\database\quick-setup.sql`
3. 点击 ▶️ 执行

### 步骤4: 运行初始化脚本
1. 左侧找到新创建的数据库 "englishai"
2. 右键 "englishai" → "Query Tool"
3. 打开文件：`D:\ENGLISH-AI\database\init.sql`
4. **删除前11行**（从 `-- 创建数据库` 到 `\c englishai;`）
5. 点击 ▶️ 执行

### 步骤5: 插入种子数据
1. 在同一个 Query Tool 中
2. 打开文件：`D:\ENGLISH-AI\database\seed.sql`
3. **删除前4行**（到 `\c englishai;`）
4. 点击 ▶️ 执行

### 步骤6: 验证
运行以下 SQL：
```sql
SELECT COUNT(*) FROM quest_templates;
```
应该显示：`5`（5个新手村任务）

---

## ✅ 完成！

现在可以启动项目：
```bash
npm run dev
```

---

## 配置信息

```
数据库名: englishai
用户名:   englishai
密码:     Englishai
主机:     localhost
端口:     5432
```

后端配置文件 `backend/.env` 已经配置好了。
